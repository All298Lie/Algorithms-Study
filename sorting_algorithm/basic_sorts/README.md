# 단순/기초 정렬 (Basic Sorts)

# 버블 정렬 (Bubble Sort)

## 1. 개요 및 복잡도
### 시간 복잡도
최선 : $O(n)$ (이미 정렬된 경우)  
평균 : $O(n^2)$  
최악 : $O(n^2)$

### 공간 복잡도
$O(1)$ (추가 메모리를 거의 사용하지 않음)

### 핵심 요약
- 인접한 두 원소를 비교하여 순서가 잘못되어 있다면 교환하는 과정을 반복하여 정렬하는 방식.
- 큰 값이 거품처럼 수면 위로 떠오르는 듯한 모습

## 2. 동작 원리
1. 첫번째 원소부터 인접한 원소와 크기를 비교한다.
2. 앞의 원소가 뒤의 원소보다 크다면 두 원소의 자리를 바꾼다.
3. 배열의 끝까지 이동하며 이 과정을 수행하면, 가장 큰 원소가 맨 뒤로 이동하여 고정된다.
4. 고정된 맨 뒷자리를 제외하고, 나머지 원소들에 대해 위 과정을 반복한다.
5. 더 이상 교환이 일어나지 않을 때까지 반복한다.

## 3. 장단점 및 특징
### 장점
- 구현이 매우 간단하고 직관적이다.  
- 코드를 이해하기 쉽다.

### 단점
- $O(n^2)$의 시간 복잡도를 가지므로 데이터 양이 많아지면 성능이 급격히 저하된다.  
- 실제 상용 코드에서는 거의 쓰이지 않는다.

### 특이사항
- 안정 정렬 : 값이 같은 원소끼리는 순서가 유지된다.  
- In-place Sort : 제자리 정렬로, 추가적인 메모리 공간이 필요 없다.

## 4. 구현 (C++)
[BubbleSort.cpp 전체 코드 파일 확인](./BubbleSort.cpp)
```cpp
#include <vector>
#include <algorithm> // swap 함수 사용

using namespace std;

void BubbleSort(vector<int>& arr)
{
		int n = arr.size();
	
		// i : 고정될 뒷 부분의 시작 인덱스를 결정하기 위한 루프
		for (int i = 0; i < n - 1; i++)
		{
				bool isSwapped = false; // 이번 턴에 교환이 있었는지 확인
		
				// j : 실제 비교 및 교환을 수행하는 루프 (이미 정렬된 뒤쪽은 제외 : n - 1 - j)
				for (int j = 0; j < n - 1 - i; j++)
				{
						if (arr[j] > arr[j + 1])
						{
								swap(arr[j], arr[j + 1]);
								isSwapped = true;
						}
				}
		
				// 이번 회차에서 교환이 한 번도 없었다면 이미 정렬된 상태이므로 루프 탈출
				if (!isSwapped) break;
		}
}
```

---

# 선택 정렬 (Selection Sort)

## 1. 개요 및 복잡도
### 시간 복잡도

### 공간 복잡도

### 핵심 요약

## 2. 동작 원리

## 3. 장단점 및 특징
### 장점

### 단점

### 특이사항

## 4. 구현 (C++)

---

# 삽입 정렬 (Insertion Sort)

## 1. 개요 및 복잡도
### 시간 복잡도

### 공간 복잡도

### 핵심 요약

## 2. 동작 원리

## 3. 장단점 및 특징
### 장점

### 단점

### 특이사항

## 4. 구현 (C++)